.app-shell {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

.topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 28px;
  border-bottom: 1px solid var(--rule);
  background: var(--paper);
}
.topbar .brand {
  font-family: var(--font-display);
  font-size: 18px;
  font-weight: 600;
}
.topbar .user-area {
  display: flex;
  align-items: center;
  gap: 16px;
}
.topbar .user-name {
  font-size: 14px;
  color: var(--muted);
}

.progress-strip {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 12px 28px;
  background: var(--paper-raised);
  border-bottom: 1px solid var(--rule);
  font-size: 13px;
  color: var(--muted);
}
.progress-bar {
  flex: 1;
  height: 8px;
  border-radius: 5px;
  background: var(--rule);
  overflow: hidden;
  display: flex;
}
.progress-bar .seg-known { background: var(--moss); height: 100%; }
.progress-bar .seg-learning { background: var(--gold); height: 100%; }
.legend { display: flex; gap: 14px; white-space: nowrap; }
.legend span { display: inline-flex; align-items: center; gap: 6px; }
.legend .dot { width: 8px; height: 8px; border-radius: 50%; display: inline-block; }
.dot-known { background: var(--moss); }
.dot-learning { background: var(--gold); }
.dot-new { background: var(--rule); }

.study-area {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 32px 20px 56px;
}

.card-zone {
  width: 100%;
  max-width: 560px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.card-counter {
  font-size: 13px;
  color: var(--muted);
  margin-bottom: 14px;
  font-family: var(--font-mono);
}

.flip-card {
  width: 100%;
  height: 360px;
  perspective: 1400px;
  cursor: pointer;
}
.flip-card-inner {
  position: relative;
  width: 100%;
  height: 100%;
  transition: transform 0.5s cubic-bezier(.4,.2,.2,1);
  transform-style: preserve-3d;
}
.flip-card.is-flipped .flip-card-inner {
  transform: rotateY(180deg);
}
.card-face {
  position: absolute;
  inset: 0;
  backface-visibility: hidden;
  border-radius: 16px;
  background: var(--paper-raised);
  border: 1px solid var(--rule);
  box-shadow: 0 8px 24px rgba(43,38,32,0.08), 0 1px 0 rgba(43,38,32,0.04);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40px 36px;
  text-align: center;
}
.card-face-back {
  transform: rotateY(180deg);
  background: var(--moss-deep);
  color: var(--paper);
  border-color: var(--moss-deep);
}

.card-eyebrow {
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--muted);
  margin-bottom: 18px;
}
.card-face-back .card-eyebrow {
  color: rgba(250,246,238,0.6);
}

.card-word {
  font-family: var(--font-display);
  font-size: 40px;
  font-weight: 600;
  margin-bottom: 10px;
  line-height: 1.1;
}
.card-pron {
  font-family: var(--font-mono);
  font-size: 16px;
  color: var(--clay-deep);
  margin-bottom: 22px;
}
.card-explanation {
  font-size: 15px;
  line-height: 1.55;
  color: var(--ink);
  max-width: 420px;
}

.card-face-back .card-meaning {
  font-family: var(--font-display);
  font-size: 30px;
  font-weight: 600;
  margin-bottom: 18px;
}
.card-face-back .card-example {
  font-size: 15px;
  line-height: 1.6;
  color: rgba(250,246,238,0.88);
  max-width: 420px;
  font-style: italic;
}

.flip-hint {
  margin-top: 14px;
  font-size: 13px;
  color: var(--muted);
}

.answer-buttons {
  display: flex;
  gap: 14px;
  margin-top: 26px;
  width: 100%;
}
.answer-buttons .btn {
  flex: 1;
  padding: 16px 0;
  font-size: 15px;
}
.btn-again {
  background: transparent;
  border: 1.5px solid var(--clay);
  color: var(--clay-deep);
}
.btn-again:hover { background: rgba(181,86,58,0.07); }
.btn-know {
  background: var(--moss-deep);
  color: var(--paper);
  border: 1.5px solid var(--moss-deep);
}
.btn-know:hover { background: var(--moss); }

.empty-state, .done-state {
  text-align: center;
  max-width: 420px;
}
.empty-state h2, .done-state h2 {
  font-size: 26px;
  margin-bottom: 12px;
}
.empty-state p, .done-state p {
  color: var(--muted);
  line-height: 1.6;
  margin-bottom: 24px;
}

.loading-state {
  color: var(--muted);
  font-size: 14px;
}

@media (max-width: 480px) {
  .flip-card { height: 400px; }
  .card-word { font-size: 32px; }
  .card-face { padding: 28px 22px; }
  .topbar { padding: 14px 18px; }
  .progress-strip { padding: 10px 18px; flex-wrap: wrap; }
}
